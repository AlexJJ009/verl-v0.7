# Active Plan Index

This index is branch-local for `feature/on-policy-wdl-sft-dual-rollout`.

## Active

| Plan | Purpose | Status |
|---|---|---|
| `dual_submodel_rollout_wdl_sft.md` | Dual-submodel rollout WDL-SFT plan and algorithm boundary. | Implemented, but 3A real training failed as method-level negative result; keep as the branch's main historical/active context. |
| `dual_submodel_rollout_wdl_sft_status.md` | Status notes for the dual-submodel rollout work. | Active status reference. |
| `wdl_sft_is.md` | WDL-SFT v2 loss plan inherited from the parent branch. | Background / still relevant where the dual branch uses v2 loss semantics. |
| `ablation_single_model.md` | Single-model ablation plan inherited from the parent branch. | Background / related baseline context. |
| `on_policy_wdl_sft.md` | Original on-policy WDL-SFT plan. | Historical context. |
| `dual_submodel_rollout_wdl_sft_3a_failure_analysis.md` | Failure analysis for the first dual-submodel real run. | Active method-level negative-result reference. |

When adding or retiring active work, update this index and this branch's
`CLAUDE.md`.
