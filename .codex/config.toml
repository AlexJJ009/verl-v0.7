# Generated by scripts/sync_agent_context.py; edit .claude/hooks.json or .codex/hooks.json, then rerun this script.
# Codex-native project hook config generated from mirrored hooks.

[features]

hooks = true
remote_control = true
[hooks]
PreToolUse = [{ matcher = "Bash", hooks = [{ type = "command", command = ".codex/hooks/pre-commit-tests.sh", timeout = 120, async = false }] }]
