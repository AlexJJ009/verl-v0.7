[project]
name = "on-policy-wdl-viz"
version = "0.1.0"
description = "Offline plotting utilities for On-Policy WDL-SFT staged experiments."
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
]

[tool.uv]
package = false
